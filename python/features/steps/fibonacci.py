from behave import *

@when(u'the user enters the value 1')
def step_impl(context):
    assert False

@then(u'the program output should be 1')
def step_impl(context):
    assert False

# vim: expandtab shiftwidth=4 softtabstop=4
